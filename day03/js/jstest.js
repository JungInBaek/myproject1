let product = 1000;
let count = 5;
console.log("물건 전체 가격 : " + product * count);

let totalPrice = 1000;
let price = 200;
console.log("물건의 개수 : " + totalPrice / price);

let apple = 2000;
let pear = 3000;
console.log(
  "사과 구매 금액이 배 구매 금액보다 높을까? : " + (apple * 3 > pear * 2)
);

let yesterday = 25;
let today = 10;
console.log("어제와 오늘의 평균 온도는 : " + (yesterday + today) / 2);
